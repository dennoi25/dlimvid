services:
  - type: web
    name: line-media-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: LINE_TOKEN
        sync: false
